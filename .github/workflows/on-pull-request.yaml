name: Test Application
run-name: Test Application
on:
  workflow_dispatch:
  pull_request:
    branches:
      - master
      - develop
      - release/*
jobs:
  test_application:
    uses: bloiseleo/gymbro/.github/workflows/test-application.yaml@master
